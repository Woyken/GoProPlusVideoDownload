import React from 'React';

interface Props {}

function VideoListElement(props: Props): JSX.Element {
    return (
        <div>
            <div></div>
        </div>
    );
}

export default VideoListElement;
